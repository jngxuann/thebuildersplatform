import { useEffect, useRef, useState } from 'react'
import './App.css'

const STORAGE_KEY = 'thebuildersplatform.v1'
const STAGES = { idea: 'Idea', prototype: 'Prototype', pilot: 'Pilot', launched: 'Launched' }
const NEEDS = { builders: 'Builders', testers: 'Testers', mentor: 'Mentor', funding: 'Funding' }
const ROLES = { student: 'Student', mentor: 'Mentor', sponsor: 'Sponsor' }
const SCHOOLS = { NUS: 'NUS', NTU: 'NTU', SUTD: 'SUTD', SP: 'Singapore Polytechnic' }

const seedProjects = [
  { id: 'p1', title: 'RoomRadar SG', pitch: 'Live map of empty study rooms across campus.', description: 'RoomRadar combines room booking data with crowd-sourced check-ins so students can find a quiet seat in under a minute at NUS Kent Ridge and UTown.', owner: 'Marcus Lim', github: 'https://github.com/thebuildersplatform/roomradar-sg', demo: '', stage: 'pilot', needs: ['builders', 'testers'], goal: 0, funded: 0, builders: ['Marcus Lim', 'Aisha K.'], testers: ['Ben T.'], mentors: [], sponsors: [], tags: ['mobile', 'maps', 'education'] },
  { id: 'p2', title: 'Canteen Queue Predictor', pitch: 'Know which NUS Engineering stall has the shortest line before you walk over.', description: 'A student-built model using campus Wi-Fi density as a proxy for crowd levels. The next test is validating predictions against real queue times at Kent Ridge and UTown.', owner: 'Priya Nair', github: '', demo: 'https://queue-predictor-demo.vercel.app', stage: 'prototype', needs: ['builders', 'testers', 'mentor'], goal: 0, funded: 0, builders: ['Priya Nair'], testers: [], mentors: [], sponsors: [], tags: ['data', 'AI', 'smart campus'] },
  { id: 'p3', title: 'LabShare', pitch: 'Borrow idle lab equipment from other Singapore departments.', description: 'An inventory of equipment that sits unused most of the week: oscilloscopes, 3D printers and sensor kits. Three departments are on board; the booking flow needs work.', owner: 'Wei Jun', github: 'https://github.com/thebuildersplatform/labshare', demo: '', stage: 'pilot', needs: ['builders', 'funding', 'mentor'], goal: 1500, funded: 400, builders: ['Wei Jun', 'Tan S.'], testers: [], mentors: ['Dr Rahman'], sponsors: [], tags: ['hardware', 'ops', 'makerspace'] },
  { id: 'p4', title: 'SilverSafe', pitch: 'A discreet fall-detection check-in for seniors living alone.', description: 'SilverSafe pairs low-cost sensors with family and community-care alerts. The team is exploring a pilot with a Queenstown active ageing centre.', owner: 'Aisha K.', github: 'https://github.com/thebuildersplatform/silversafe', demo: '', stage: 'idea', needs: ['builders', 'mentor', 'funding'], goal: 3000, funded: 0, builders: ['Aisha K.'], testers: [], mentors: [], sponsors: [], tags: ['healthtech', 'ageing', 'social impact'] },
  { id: 'p5', title: 'Grant Finder SG', pitch: 'One search box for student grants and seed funds in Singapore.', description: 'A maintained directory of university, youth and startup funding programmes, tagged by eligibility, focus area and deadline.', owner: 'Ben T.', github: 'https://github.com/thebuildersplatform/grant-finder-sg', demo: 'https://grant-finder-sg.vercel.app', stage: 'launched', needs: ['builders'], goal: 0, funded: 0, builders: ['Ben T.', 'Priya Nair'], testers: ['Wei Jun', 'Aisha K.'], mentors: ['J Ong'], sponsors: [], tags: ['web', 'funding', 'student life'] },
  { id: 'p6', title: 'Solar Bench', pitch: 'An outdoor bench that charges your laptop from a panel on its back.', description: 'Prototype v1 works but the battery lasts only 40 minutes. The team needs an electrical engineering hand and funding for a better cell.', owner: 'Tan S.', github: '', demo: 'https://solar-bench-demo.vercel.app', stage: 'prototype', needs: ['builders', 'funding', 'testers'], goal: 2000, funded: 1250, builders: ['Tan S.'], testers: [], mentors: [], sponsors: ['Alumni Circle', 'GreenTech SG'], tags: ['hardware', 'energy', 'sustainability'] },
  { id: 'p7', title: 'StepFree SG', pitch: 'Accessible routes between lecture halls, MRT stations and clinics.', description: 'A community-maintained map of step-free and lower-sensory paths around Singapore campuses and neighbourhoods.', owner: 'J Ong', github: 'https://github.com/thebuildersplatform/stepfree-sg', demo: '', stage: 'pilot', needs: ['testers', 'mentor'], goal: 0, funded: 0, builders: ['J Ong', 'Ben T.'], testers: [], mentors: [], sponsors: [], tags: ['accessibility', 'maps', 'community'] },
  { id: 'p8', title: 'Smart Locker System', pitch: 'Reserve shared equipment lockers at polytechnic and university hubs.', description: 'A simple booking and access workflow for project teams sharing cameras, microcontrollers and prototyping kits across campus.', owner: 'Nadia R.', github: 'https://github.com/thebuildersplatform/smart-locker', demo: '', stage: 'idea', needs: ['builders', 'mentor'], goal: 0, funded: 0, builders: ['Nadia R.'], testers: [], mentors: [], sponsors: [], tags: ['hardware', 'campus', 'smart nation'] },
]

const mentors = [
  { id: 'm1', name: 'Dr Nur Rahman', title: 'Biomedical Engineering Lead', organisation: 'A*STAR', skills: ['Biomedical engineering', 'Hardware engineering'], specialties: ['Medtech', 'Healthtech'], industries: ['Healthcare', 'Deep tech'], stages: ['idea', 'prototype', 'pilot'], bio: 'Works with student teams turning clinical needs into practical, testable devices.', availability: '2 sessions per month', link: 'https://www.linkedin.com/in/example-nur-rahman' },
  { id: 'm2', name: 'Melissa Tan', title: 'Product Director', organisation: 'GovTech Singapore', skills: ['Product management', 'UX/UI'], specialties: ['Smart Nation', 'Social impact'], industries: ['Public services', 'Education'], stages: ['idea', 'prototype', 'pilot'], bio: 'Helps teams make useful products for real people, especially in civic and campus settings.', availability: 'Monthly office hours', link: 'https://www.linkedin.com/in/example-melissa-tan' },
  { id: 'm3', name: 'Arjun Menon', title: 'Principal ML Engineer', organisation: 'Sea Group', skills: ['AI / machine learning', 'Software engineering'], specialties: ['AI', 'Data products'], industries: ['Fintech', 'Consumer technology'], stages: ['prototype', 'pilot', 'launched'], bio: 'Supports teams with responsible model design, evaluation and building reliable data products.', availability: 'Quarterly cohort', link: 'https://www.linkedin.com/in/example-arjun-menon' },
  { id: 'm4', name: 'Chloe Lim', title: 'Impact Investment Associate', organisation: 'Temasek Foundation', skills: ['Startup fundraising', 'Partnerships'], specialties: ['Sustainability', 'Social impact'], industries: ['Climate', 'Community care'], stages: ['pilot', 'launched'], bio: 'Guides founders through impact measurement, partnerships and early funding conversations.', availability: 'Accepting introductions', link: 'https://www.linkedin.com/in/example-chloe-lim' },
  { id: 'm5', name: 'Daniel Koh', title: 'Cybersecurity Consultant', organisation: 'NCS', skills: ['Cybersecurity', 'Software engineering'], specialties: ['Cybersecurity', 'Privacy'], industries: ['Fintech', 'Digital health'], stages: ['prototype', 'pilot'], bio: 'Helps student builders threat-model their products before they reach real users.', availability: 'One project per semester', link: '' },
]

const resources = [
  { id: 'r1', name: 'NUS MakerSpace', location: 'NUS Kent Ridge, Engineering Block EA', address: '9 Engineering Drive 1, Singapore 117575', category: 'Makerspaces', filter: 'fabrication', school: 'NUS', equipment: '3D printers, laser cutter, soldering stations, hand tools', access: 'Mon-Fri, 10am-6pm; induction required', users: 'NUS students and staff', booking: 'Book a machine slot through the NUS MakerSpace portal', link: 'https://www.eng.nus.edu.sg/nusmakerspace/', description: 'A practical build space for student hardware, product and fabrication projects.' },
  { id: 'r2', name: 'The Hive Collaborative Space', location: 'NTU North Spine', address: '50 Nanyang Avenue, Singapore 639798', category: 'Collaboration spaces', filter: 'collaboration', school: 'NTU', equipment: 'Whiteboards, project tables, presentation screens', access: 'Campus access during building hours', users: 'NTU students, clubs and approved project teams', booking: 'Reserve rooms through NTU room booking', link: 'https://www.ntu.edu.sg/life-at-ntu/student-life/facilities', description: 'Flexible tables and rooms for teams moving from idea to prototype.' },
  { id: 'r3', name: 'SUTD FabLab', location: 'SUTD Upper Changi Campus', address: '8 Somapah Road, Singapore 487372', category: 'Fabrication tools', filter: 'fabrication', school: 'SUTD', equipment: 'CNC router, 3D printers, electronics benches, woodworking tools', access: 'Weekdays and selected Saturday workshops', users: 'SUTD community; external student teams by arrangement', booking: 'Safety induction and project approval required', link: 'https://www.sutd.edu.sg/campus-life/fablab/', description: 'A serious prototyping facility for physical computing and product design.' },
  { id: 'r4', name: 'Singapore Polytechnic Makerspace', location: 'SP Design School', address: '500 Dover Road, Singapore 139651', category: '3D printers', filter: '3d', school: 'SP', equipment: 'FDM and resin printers, scanner, modelling workstations', access: 'Mon-Fri, 9am-5pm during term', users: 'SP students and approved partner teams', booking: 'Submit a print request and complete training', link: 'https://www.sp.edu.sg/', description: 'Accessible rapid-prototyping support for student products and assistive devices.' },
  { id: 'r5', name: 'LaunchPad @ one-north', location: 'JTC LaunchPad, Block 73', address: '73 Ayer Rajah Crescent, Singapore 139952', category: 'Workspaces with monitors', filter: 'workspace', school: null, equipment: 'Hot desks, monitors, meeting rooms, video conferencing', access: 'Weekdays, 9am-7pm; community events after hours', users: 'Startup teams, student founders and ecosystem partners', booking: 'Register for a desk or attend an open community session', link: 'https://www.jtc.gov.sg/industrial-space/list-of-industrial-space/launchpad-one-north', description: 'A startup ecosystem space near Fusionopolis for teams needing a professional base.' },
  { id: 'r6', name: 'NUS Electronics Lab', location: 'NUS Engineering, E4', address: '4 Engineering Drive 3, Singapore 117583', category: 'Electronics benches', filter: 'electronics', school: 'NUS', equipment: 'Oscilloscopes, power supplies, logic analysers and soldering tools', access: 'Lab hours vary by module and supervisor', users: 'NUS engineering students and approved project teams', booking: 'Supervisor approval and lab induction required', link: 'https://www.eng.nus.edu.sg/ece/', description: 'A supervised electronics workspace for sensing, robotics and embedded projects.' },
]

const funding = [
  { id: 'f1', name: 'NUS Student Venture Seed Grant', provider: 'NUS Enterprise', amount: 'Up to S$5,000', eligibility: 'NUS students and recent graduates with a faculty or enterprise sponsor', stage: 'idea', sectors: ['student', 'startup', 'education'], deadline: 'Rolling', description: 'Early support for validating a student idea, interviewing users and building a first prototype.', link: 'https://enterprise.nus.edu.sg', tags: ['university', 'seed', 'student'] },
  { id: 'f2', name: 'Startup SG Founder', provider: 'Enterprise Singapore', amount: 'Up to S$50,000', eligibility: 'First-time Singapore citizen founders through accredited mentor partners', stage: 'prototype', sectors: ['startup', 'deep tech'], deadline: 'Rolling', description: 'Mentorship and startup capital for Singapore-based founders developing a scalable venture.', link: 'https://www.startupsg.gov.sg', tags: ['startup', 'prototype', 'founder'] },
  { id: 'f3', name: 'Temasek Foundation Liveability Challenge', provider: 'Temasek Foundation', amount: 'Up to S$1,000,000', eligibility: 'Teams with a tested solution for urban sustainability and liveability', stage: 'pilot', sectors: ['sustainability', 'smart nation'], deadline: 'Annual call', description: 'Supports solutions that make Singapore and cities across Asia more liveable, resilient and sustainable.', link: 'https://www.temasekfoundation.org.sg', tags: ['sustainability', 'urban', 'impact'] },
  { id: 'f4', name: 'MOH HealthTech Challenge', provider: 'Singapore Ministry of Health', amount: 'S$20,000-S$100,000', eligibility: 'Teams with health, ageing or care solutions ready for a controlled pilot', stage: 'pilot', sectors: ['healthtech', 'medtech'], deadline: 'Check current challenge call', description: 'A pathway for testing practical digital and physical solutions with healthcare stakeholders.', link: 'https://www.moh.gov.sg', tags: ['healthcare', 'pilot', 'ageing'] },
  { id: 'f5', name: 'Youth Co:Lab Social Innovation Fund', provider: 'UNDP Singapore partners', amount: 'Up to S$10,000', eligibility: 'Youth-led teams working on community or social impact outcomes', stage: 'launched', sectors: ['social impact', 'community'], deadline: 'Annual call', description: 'Funding and support for youth teams scaling solutions that strengthen communities.', link: 'https://www.youthcolab.org', tags: ['youth', 'social impact', 'community'] },
]

const roleCopy = { student: 'Post ideas, join builds, volunteer to test', mentor: 'Share experience with Singapore student teams', sponsor: 'Back projects at any stage' }
const peopleOf = (project) => [...new Set([...(project.builders || []), ...(project.testers || []), ...(project.mentors || []), ...(project.sponsors || [])])]
const initials = (name) => name.split(/\s+/).map((part) => part[0]).join('').slice(0, 2).toUpperCase()
const getSaved = () => { try { const value = JSON.parse(window.localStorage.getItem(STORAGE_KEY)); return value || {} } catch { return {} } }

function CountUp({ end, prefix = '', suffix = '', duration = 1250 }) {
  const [displayValue, setDisplayValue] = useState(0)
  const elementRef = useRef(null)
  const animationRef = useRef(null)
  const startedRef = useRef(false)

  useEffect(() => {
    const element = elementRef.current
    if (!element || startedRef.current) return undefined

    const finish = () => {
      if (startedRef.current) return
      startedRef.current = true
      const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches
      if (reduceMotion) {
        setDisplayValue(end)
        return
      }

      const startedAt = performance.now()
      const step = (now) => {
        const progress = Math.min((now - startedAt) / duration, 1)
        const easedProgress = 1 - ((1 - progress) ** 3)
        setDisplayValue(Math.round(end * easedProgress))
        if (progress < 1) animationRef.current = window.requestAnimationFrame(step)
      }
      animationRef.current = window.requestAnimationFrame(step)
    }

    const observer = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting) {
        finish()
        observer.disconnect()
      }
    }, { threshold: 0.2 })
    observer.observe(element)

    return () => {
      observer.disconnect()
      if (animationRef.current) window.cancelAnimationFrame(animationRef.current)
    }
  }, [duration, end])

  useEffect(() => {
    if (startedRef.current) setDisplayValue(end)
  }, [end])

  return <span ref={elementRef} aria-label={`${prefix}${end.toLocaleString('en-SG')}${suffix}`}>{prefix}{displayValue.toLocaleString('en-SG')}{suffix}</span>
}

function App() {
  const saved = getSaved()
  const [user, setUser] = useState(saved.user || null)
  const [role, setRole] = useState('student')
  const [page, setPage] = useState(saved.view || 'main')
  const [projects, setProjects] = useState(saved.projects?.length ? saved.projects : seedProjects)
  const [passed, setPassed] = useState(saved.passed || [])
  const [selected, setSelected] = useState(null)
  const [toast, setToast] = useState('')

  useEffect(() => { window.localStorage.setItem(STORAGE_KEY, JSON.stringify({ user, projects, passed, view: page })) }, [page, passed, projects, user])
  const notify = (message) => { setToast(message); window.clearTimeout(notify.timer); notify.timer = window.setTimeout(() => setToast(''), 2200) }
  const updateProject = (id, changes) => setProjects((items) => items.map((item) => item.id === id ? { ...item, ...changes } : item))
  const togglePerson = (id, type) => updateProject(id, { [type]: (() => { const item = projects.find((project) => project.id === id); return item[type].includes(user.name) ? item[type].filter((name) => name !== user.name) : [...item[type], user.name] })() })
  const sponsorProject = (id, amount) => { if (!(amount > 0)) return false; setProjects((items) => items.map((item) => item.id === id ? { ...item, funded: item.funded + amount, sponsors: item.sponsors.includes(user.name) ? item.sponsors : [...item.sponsors, user.name], needs: item.goal && item.funded + amount >= item.goal ? item.needs.filter((need) => need !== 'funding') : item.needs } : item)); return true }
  const nav = user?.role === 'student' ? [['main', 'Projects'], ['upload', 'Post a Project'], ['builders', 'Find a Build'], ['testers', 'Test a Project'], ['spaces', 'Spaces & Equipment'], ['mentors', 'Mentors'], ['funding', 'Funding Opportunities']] : user?.role === 'mentor' ? [['main', 'Projects'], ['mentors', 'Mentors'], ['spaces', 'Spaces & Equipment']] : [['main', 'Projects'], ['funding', 'Funding Opportunities'], ['sponsor', 'Fund a Project']]

  if (!user) return <Signup role={role} setRole={setRole} onSubmit={(event) => { event.preventDefault(); const name = new FormData(event.currentTarget).get('name'); if (name) setUser({ name, role }) }} projects={projects} />
  return <div id="app" className="on"><Sidebar user={user} page={page} nav={nav} setPage={setPage} signOut={() => setUser(null)} /><main>
    {page === 'main' && <Projects projects={projects} user={user} open={setSelected} goUpload={() => setPage('upload')} />}
    {page === 'upload' && <Upload onSubmit={(event) => { event.preventDefault(); const data = new FormData(event.currentTarget); const github = String(data.get('github') || '').trim(); const demo = String(data.get('demo') || '').trim(); if (!github && !demo) { notify('Add a GitHub repository or prototype link'); return } const description = data.get('description') || 'A new project from the Singapore campus community.'; const project = { id: `p${Date.now()}`, title: data.get('title'), pitch: data.get('pitch'), description, owner: user.name, github, demo, stage: data.get('stage'), needs: data.getAll('need'), goal: Number(data.get('goal')) || 0, funded: 0, builders: [user.name], testers: [], mentors: [], sponsors: [], tags: data.get('tags')?.split(',').map((tag) => tag.trim()).filter(Boolean) || [] }; setProjects((items) => [project, ...items]); setPage('main'); notify('Project posted') }} />}
    {page === 'builders' && <Builders projects={projects} user={user} passed={passed} onDecision={(project, join) => { if (join) updateProject(project.id, { builders: [...new Set([...project.builders, user.name])] }); else setPassed((items) => [...new Set([...items, project.id])]); notify(join ? `Joined ${project.title}` : `Passed on ${project.title}`) }} open={setSelected} />}
    {page === 'testers' && <ParticipationPage kind="testers" title="Test a Project" description="Projects from Singapore student teams looking for a second pair of eyes." projects={projects.filter((project) => project.needs.includes('testers') && project.stage !== 'launched')} user={user} open={setSelected} toggle={(project) => { togglePerson(project.id, 'testers'); notify(`${project.testers.includes(user.name) ? 'Withdrawn from' : 'You are testing'} ${project.title}`) }} />}
    {page === 'spaces' && <Spaces />}
    {page === 'mentors' && <MentorDirectory projects={projects} user={user} openProject={setSelected} />}
    {page === 'funding' && <FundingOpportunities />}
    {page === 'sponsor' && <SponsorProjects projects={projects} open={setSelected} sponsor={sponsorProject} notify={notify} />}
  </main>{selected && <ProjectModal project={selected} user={user} close={() => setSelected(null)} updateProject={updateProject} togglePerson={togglePerson} sponsor={sponsorProject} notify={notify} />}{toast && <div id="toast" className="on">{toast}</div>}</div>
}

function Signup({ role, setRole, onSubmit, projects }) { const people = new Set(projects.flatMap(peopleOf)).size; const openRoles = projects.reduce((total, project) => total + project.needs.length, 0); const fundingSought = projects.reduce((total, project) => total + project.goal, 0); return <section id="signup"><div className="left"><div className="wordmark"><img src="/whitespace.png" alt="Whitespace logo" title="Whitespace" />thebuildersplatform</div><div><h1>Build something useful for <mark>Singapore</mark>.</h1><p>Student ideas, practical spaces and the people who can help take them from a first sketch to something real.</p></div><div className="stat-row"><div><b><CountUp end={projects.length} /></b>projects live</div><div><b><CountUp end={people} /></b>people involved</div><div><b><CountUp end={openRoles} /></b>open roles</div><div><b><CountUp end={mentors.length} /></b>mentors</div><div><b><CountUp end={resources.length} /></b>spaces & equipment</div><div><b><CountUp end={fundingSought} prefix="S$" /></b>funding sought</div></div></div><div className="right"><form onSubmit={onSubmit}><h2>Join the Singapore campus</h2><p className="hint" style={{ marginBottom: 22 }}>Choose how you want to contribute. You can switch roles by signing out.</p><div className="field"><label htmlFor="name">Your name</label><input id="name" name="name" placeholder="e.g. Priya Nair" autoComplete="name" required /></div><div className="roles" role="group" aria-label="Choose a role">{Object.entries(ROLES).map(([id, label]) => <button type="button" className="role" data-role={id} aria-pressed={role === id} key={id} onClick={() => setRole(id)}><span className="sw">{id === 'sponsor' ? '$' : label[0]}</span><span><strong>{label}</strong><span>{roleCopy[id]}</span></span></button>)}</div><button className="btn primary" type="submit" style={{ width: '100%', justifyContent: 'center', padding: 12 }}>Enter thebuildersplatform</button></form></div></section> }
function Sidebar({ user, page, nav, setPage, signOut }) { return <aside><div className="wordmark"><img src="/whitespace.png" alt="Whitespace logo" title="Whitespace" />thebuildersplatform</div><nav>{nav.map(([id, label]) => <button key={id} className={page === id ? 'active' : ''} onClick={() => setPage(id)}>{label}</button>)}</nav><div className="me"><b>{user.name}</b><span>{ROLES[user.role]}</span><div><button className="btn quiet small" onClick={signOut}>Sign out</button></div></div></aside> }
function PageHead({ title, description, action }) { return <div className="pagehead"><div><h1>{title}</h1><p>{description}</p></div>{action}</div> }
function FilterBar({ options, value, onChange, search, setSearch }) { return <div className="toolbar">{options.map(([id, label]) => <button className={`chip ${value === id ? 'on' : ''}`} key={id} onClick={() => onChange(id)}>{label}</button>)}{setSearch && <input className="search" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search" aria-label="Search" />}</div> }
function Projects({ projects, user, open, goUpload }) { const [stage, setStage] = useState('all'); const [search, setSearch] = useState(''); const visible = projects.filter((project) => (stage === 'all' || project.stage === stage) && `${project.title} ${project.pitch} ${project.description} ${project.owner} ${project.tags.join(' ')}`.toLowerCase().includes(search.toLowerCase())); return <section className="page on"><PageHead title="Projects" description="Singapore student projects moving from idea to impact." action={user.role === 'student' && <button className="btn primary" onClick={goUpload}>Post a Project</button>} /><FilterBar options={[['all', 'All'], ...Object.entries(STAGES)]} value={stage} onChange={setStage} search={search} setSearch={setSearch} /><div className="grid">{visible.length ? visible.map((project) => <ProjectCard key={project.id} project={project} user={user} open={() => open(project)} />) : <div className="empty"><b>No projects found</b>Try another stage or search term.</div>}</div></section> }
function ProjectCard({ project, user, open }) { const people = peopleOf(project); const mine = project.owner === user.name || people.includes(user.name); return <article className="card" onClick={open} onKeyDown={(event) => event.key === 'Enter' && open()} tabIndex="0"><span className={`status ${project.stage}`}>{STAGES[project.stage]}</span><h3>{project.title}</h3><p>{project.pitch}</p><div className="needs">{project.needs.map((need) => <span className={`need ${need === 'builders' ? 'hot' : ''}`} key={need}>{NEEDS[need]}</span>)}{mine && <span className="need" style={{ background: 'var(--blue-soft)', color: 'var(--blue)' }}>You&apos;re on this</span>}</div><div className="meta"><span>by {project.owner}</span><span className="people">{people.slice(0, 4).map((person) => <span className="av" title={person} key={person}>{initials(person)}</span>)}</span></div><div className="hint">{project.github ? 'GitHub linked' : 'Demo linked'}</div></article> }
function Upload({ onSubmit }) { return <section className="page on"><PageHead title="Post a Project" description="Share a Singapore problem worth solving. Every project needs a repository or working demo link." /><div className="form-2col"><form onSubmit={onSubmit}><div className="field"><label htmlFor="u-title">Title</label><input id="u-title" name="title" required placeholder="What are you building?" /></div><div className="field"><label htmlFor="u-pitch">One-line pitch</label><input id="u-pitch" name="pitch" required maxLength="120" placeholder="Explain it to a stranger in the lift" /></div><div className="field"><label htmlFor="u-desc">Description</label><textarea id="u-desc" name="description" placeholder="What is the Singapore problem, what is done, and what is next?" /></div><div className="field"><label htmlFor="u-github">GitHub repository</label><input id="u-github" name="github" type="url" placeholder="https://github.com/you/project" /></div><div className="field"><label htmlFor="u-demo">Prototype / demo link</label><input id="u-demo" name="demo" type="url" placeholder="https://your-demo.vercel.app" /><div className="hint">At least one link is required.</div></div><div className="field"><label htmlFor="u-stage">Project stage</label><select id="u-stage" name="stage" defaultValue="idea">{Object.entries(STAGES).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></div><div className="field"><label>What do you need?</label><div className="checks">{Object.entries(NEEDS).map(([value, label]) => <label className="check" key={value}><input type="checkbox" name="need" value={value} defaultChecked={value === 'builders'} /> {label}</label>)}</div></div><div className="field"><label htmlFor="u-goal">Funding goal (S$)</label><input id="u-goal" name="goal" type="number" min="0" step="50" defaultValue="0" /></div><div className="field"><label htmlFor="u-tags">Tags</label><input id="u-tags" name="tags" placeholder="e.g. healthtech, ageing" /></div><button className="btn primary" type="submit">Post Project</button></form><div className="preview"><div className="hint">Live preview</div><div className="card"><h3>Your project will appear here</h3><p>Complete the form to introduce your project to the Singapore building community.</p></div></div></div></section> }
function Builders({ projects, user, passed, onDecision, open }) { const project = projects.find((item) => item.stage !== 'launched' && item.needs.includes('builders') && item.owner !== user.name && !item.builders.includes(user.name) && !passed.includes(item.id)); const joined = projects.filter((item) => item.builders.includes(user.name) && item.owner !== user.name); return <section className="page on"><PageHead title="Find a Build" description="Discover Singapore campus projects looking for builders." /><div className="deck-wrap"><div><div className="deck">{project ? <div className="swipe"><span className={`status ${project.stage}`}>{STAGES[project.stage]}</span><h2>{project.title}</h2><p className="lead">{project.pitch}</p><div className="body">{project.description}</div><div className="needs">{project.needs.map((need) => <span className="need" key={need}>{NEEDS[need]}</span>)}</div><div className="meta"><span>by {project.owner}</span><span>{project.builders.length} on the team</span></div></div> : <div className="empty"><b>You&apos;re all caught up</b>Check back when a new Singapore build is posted.</div>}</div><div className="deck-controls"><button className="btn" disabled={!project} onClick={() => onDecision(project, false)}>Pass</button><button className="btn quiet" disabled={!project} onClick={() => open(project)}>Details</button><button className="btn mark" disabled={!project} onClick={() => onDecision(project, true)}>Join this build</button></div></div><div className="side"><h3>Builds you&apos;ve joined</h3><ul>{joined.map((item) => <li key={item.id}><span>{item.title}</span><span className="hint">{item.owner}</span></li>)}</ul>{!joined.length && <p className="hint">Nothing yet. Join a build to see it here.</p>}<p className="hint" style={{ marginTop: 14 }}><span className="kbd">←</span> pass &nbsp; <span className="kbd">→</span> join</p></div></div></section> }
function ParticipationPage({ kind, title, description, projects, user, open, toggle }) { return <section className="page on"><PageHead title={title} description={description} /><div className="rows">{projects.map((project) => { const active = project[kind].includes(user.name); return <div className="row" key={project.id}><div><div className="t"><h3>{project.title}</h3><span className={`status ${project.stage}`}>{STAGES[project.stage]}</span></div><p>{project.pitch}</p><div className="sub">{project[kind].length} participant{project[kind].length === 1 ? '' : 's'} so far · by {project.owner}</div></div><div style={{ display: 'flex', gap: 8 }}><button className="btn small" onClick={() => open(project)}>Details</button><button className={`btn small ${active ? '' : 'primary'}`} onClick={() => toggle(project)}>{active ? 'Withdraw' : 'Volunteer'}</button></div></div>})}</div></section> }

function Spaces() { const [school, setSchool] = useState('all'); const [filter, setFilter] = useState('all'); const [search, setSearch] = useState(''); const options = [['all', 'All'], ['collaboration', 'Collaboration'], ['3d', '3D Printing'], ['electronics', 'Electronics'], ['workspace', 'Workspace'], ['fabrication', 'Fabrication']]; const matchesSchool = (resource) => school === 'all' ? true : school === 'none' ? resource.school === null : (resource.school === school || resource.school === null); const visible = resources.filter((resource) => matchesSchool(resource) && (filter === 'all' || resource.filter === filter) && `${resource.name} ${resource.location} ${resource.category} ${resource.equipment}`.toLowerCase().includes(search.toLowerCase())); return <section className="page on"><PageHead title="Spaces & Equipment" description="Find places across Singapore to meet, make, test and ship your next project." /><div className="toolbar school-row"><label htmlFor="s-school" className="hint">Where are you studying?</label><select id="s-school" className="school-select" value={school} onChange={(event) => setSchool(event.target.value)}><option value="all">All schools</option>{Object.entries(SCHOOLS).map(([value, label]) => <option value={value} key={value}>{label}</option>)}<option value="none">Not currently a student</option></select></div><FilterBar options={options} value={filter} onChange={setFilter} search={search} setSearch={setSearch} /><div className="grid resource-grid">{visible.length ? visible.map((resource) => <ResourceCard resource={resource} key={resource.id} />) : <div className="empty"><b>No spaces found</b>Try another school, category or search term.</div>}</div></section> }
function ResourceCard({ resource }) { return <article className="card resource-card"><span className="need hot">{resource.category}</span><h3>{resource.name}</h3><p>{resource.description}</p><div className="resource-detail"><b>{resource.location}</b><span>{resource.address}</span></div><div className="resource-detail"><b>Equipment</b><span>{resource.equipment}</span></div><div className="resource-detail"><b>Access</b><span>{resource.access}</span></div><div className="resource-detail"><b>Who can use it</b><span>{resource.users}</span></div><div className="resource-detail"><b>Booking</b><span>{resource.booking}</span>{resource.link && <a className="gh" href={resource.link} target="_blank" rel="noreferrer">Visit booking page</a>}</div></article> }

function MentorDirectory({ projects, user, openProject }) { const [specialty, setSpecialty] = useState('all'); const [industry, setIndustry] = useState('all'); const [stage, setStage] = useState('all'); const [availability, setAvailability] = useState('all'); const [selectedMentor, setSelectedMentor] = useState(null); const specialties = [...new Set(mentors.flatMap((mentor) => mentor.specialties))]; const industries = [...new Set(mentors.flatMap((mentor) => mentor.industries))]; const available = [...new Set(mentors.map((mentor) => mentor.availability))]; const visible = mentors.filter((mentor) => (specialty === 'all' || mentor.specialties.includes(specialty)) && (industry === 'all' || mentor.industries.includes(industry)) && (stage === 'all' || mentor.stages.includes(stage)) && (availability === 'all' || mentor.availability === availability)); return <section className="page on"><PageHead title="Mentors" description="People in Singapore technology, design and impact who are open to helping student teams." /><div className="toolbar mentor-filters"><select value={specialty} onChange={(event) => setSpecialty(event.target.value)}><option value="all">All specialties</option>{specialties.map((item) => <option key={item}>{item}</option>)}</select><select value={industry} onChange={(event) => setIndustry(event.target.value)}><option value="all">All domains</option>{industries.map((item) => <option key={item}>{item}</option>)}</select><select value={stage} onChange={(event) => setStage(event.target.value)}><option value="all">All stages</option>{Object.entries(STAGES).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select><select value={availability} onChange={(event) => setAvailability(event.target.value)}><option value="all">Any availability</option>{available.map((item) => <option key={item}>{item}</option>)}</select></div><div className="grid">{visible.map((mentor) => <MentorCard mentor={mentor} open={() => setSelectedMentor(mentor)} key={mentor.id} />)}</div>{selectedMentor && <MentorModal mentor={selectedMentor} projects={projects} user={user} close={() => setSelectedMentor(null)} openProject={openProject} />}</section> }
function MentorCard({ mentor, open }) { return <article className="card" onClick={open} onKeyDown={(event) => event.key === 'Enter' && open()} tabIndex="0"><span className="need hot">{mentor.availability}</span><h3>{mentor.name}</h3><p>{mentor.title} · {mentor.organisation}</p><div className="needs">{mentor.specialties.map((item) => <span className="need" key={item}>{item}</span>)}</div><div className="meta"><span>{mentor.industries.join(' · ')}</span><span>{mentor.stages.map((stage) => STAGES[stage]).join(' / ')}</span></div></article> }
function MentorModal({ mentor, projects, close, openProject }) { return <div className="overlay on" onClick={close}><div className="modal" role="dialog" aria-modal="true" onClick={(event) => event.stopPropagation()}><button className="x" onClick={close} aria-label="Close">X</button><h2>{mentor.name}</h2><p className="lead">{mentor.title} · {mentor.organisation}</p><p className="body">{mentor.bio}</p><div className="kv"><div><span>Availability</span><b>{mentor.availability}</b></div><div><span>Stages</span><b>{mentor.stages.map((stage) => STAGES[stage]).join(', ')}</b></div><div><span>Focus</span><b>{mentor.specialties.join(', ')}</b></div><div><span>Domains</span><b>{mentor.industries.join(', ')}</b></div></div><div className="needs">{mentor.skills.map((skill) => <span className="need" key={skill}>{skill}</span>)}</div><div className="actions">{mentor.link && <a className="btn" href={mentor.link} target="_blank" rel="noreferrer">View profile</a>}<button className="btn primary" onClick={() => { const project = projects.find((item) => item.needs.includes('mentor')); close(); if (project) openProject(project) }}>Find a matching project</button><button className="btn" onClick={close}>Close</button></div></div></div> }

function FundingOpportunities() { const [stage, setStage] = useState('all'); const [sector, setSector] = useState('all'); const [studentOnly, setStudentOnly] = useState(false); const sectors = [...new Set(funding.flatMap((item) => item.sectors))]; const visible = funding.filter((item) => (stage === 'all' || item.stage === stage) && (sector === 'all' || item.sectors.includes(sector)) && (!studentOnly || item.sectors.includes('student') || item.eligibility.toLowerCase().includes('student'))); return <section className="page on"><PageHead title="Funding Opportunities" description="Singapore grants and programmes to help student teams validate, prototype, pilot and scale." /><div className="toolbar funding-filters"><select value={stage} onChange={(event) => setStage(event.target.value)}><option value="all">All stages</option>{Object.entries(STAGES).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select><select value={sector} onChange={(event) => setSector(event.target.value)}><option value="all">All sectors</option>{sectors.map((item) => <option value={item} key={item}>{item}</option>)}</select><label className="check compact"><input type="checkbox" checked={studentOnly} onChange={(event) => setStudentOnly(event.target.checked)} /> Student eligible</label></div><div className="rows funding-list">{visible.map((item) => <FundingCard opportunity={item} key={item.id} />)}</div></section> }
function FundingCard({ opportunity }) { return <article className="row funding-card"><div><div className="t"><h3>{opportunity.name}</h3><span className={`status ${opportunity.stage}`}>{STAGES[opportunity.stage]}</span></div><p>{opportunity.description}</p><div className="sub">{opportunity.provider} · {opportunity.deadline}</div><div className="needs">{opportunity.tags.map((tag) => <span className="need" key={tag}>{tag}</span>)}</div></div><div className="funding-meta"><b>{opportunity.amount}</b><span>{opportunity.eligibility}</span><a className="btn primary small" href={opportunity.link} target="_blank" rel="noreferrer">Application details</a></div></article> }

function SponsorProjects({ projects, open, sponsor, notify }) { const [filter, setFilter] = useState('all'); const visible = projects.filter((project) => filter === 'all' || project.stage === filter || (filter === 'asking' && project.needs.includes('funding'))); return <section className="page on"><PageHead title="Fund a Project" description="Back Singapore student projects with funding, introductions or practical support." /><FilterBar options={[['all', 'All'], ...Object.entries(STAGES), ['asking', 'Asking for funding']]} value={filter} onChange={setFilter} /><div className="rows">{visible.map((project) => <div className="row fund" key={project.id}><div><div className="t"><h3>{project.title}</h3><span className={`status ${project.stage}`}>{STAGES[project.stage]}</span></div><p>{project.pitch}</p><div className="fundnum"><b>S${project.funded.toLocaleString()}</b><span>{project.goal ? `of S$${project.goal.toLocaleString()}` : 'raised'}</span></div></div><div>{project.goal ? <><div className="bar"><i style={{ width: `${Math.min(100, project.funded / project.goal * 100)}%` }} /></div><div className="fundnum"><span>Funding progress</span><b>{Math.round(project.funded / project.goal * 100)}%</b></div></> : <span className="hint">No goal set</span>}</div><div style={{ display: 'flex', gap: 8 }}><button className="btn small" onClick={() => open(project)}>Details</button><button className="btn small primary" onClick={() => { const amount = Number(window.prompt('Amount (S$)', '500')); if (amount > 0 && sponsor(project.id, amount)) notify(`Sponsored ${project.title} with S$${amount.toLocaleString()}`) }}>Sponsor</button></div></div>)}</div></section> }

function ProjectModal({ project, user, close, updateProject, togglePerson, sponsor, notify }) { const [amount, setAmount] = useState('500'); const people = peopleOf(project); const owner = project.owner === user.name; return <div className="overlay on" onClick={close}><div className="modal" role="dialog" aria-modal="true" onClick={(event) => event.stopPropagation()}><button className="x" onClick={close} aria-label="Close">X</button><div className="needs"><span className={`status ${project.stage}`} style={{ position: 'static' }}>{STAGES[project.stage]}</span>{project.tags.map((tag) => <span className="pill" key={tag}>{tag}</span>)}</div><h2>{project.title}</h2><p className="lead">{project.pitch}</p><div className="kv"><div><span>Owner</span><b>{project.owner}</b></div><div><span>Team</span><b>{project.builders.length}</b></div><div><span>Testers</span><b>{project.testers.length}</b></div><div><span>Funding</span><b>{project.goal ? `S$${project.funded.toLocaleString()} / S$${project.goal.toLocaleString()}` : '—'}</b></div></div><p className="body">{project.description}</p><div className="link-list">{project.github && <a className="gh" href={project.github} target="_blank" rel="noreferrer">GitHub repository</a>}{project.demo && <a className="gh" href={project.demo} target="_blank" rel="noreferrer">Working prototype / demo</a>}</div><div className="needs" style={{ marginTop: 18 }}>{project.needs.map((need) => <span className="need hot" key={need}>{NEEDS[need]}</span>)}</div><div className="team">{people.map((person) => <span className="pill" key={person}>{person}</span>)}</div><div className="actions">{user.role === 'student' && project.needs.includes('builders') && !project.builders.includes(user.name) && <button className="btn mark" onClick={() => { updateProject(project.id, { builders: [...project.builders, user.name] }); notify(`Joined ${project.title}`); close() }}>Join this build</button>}{user.role === 'student' && project.needs.includes('testers') && <button className="btn" onClick={() => { togglePerson(project.id, 'testers'); notify('Tester status updated'); close() }}>{project.testers.includes(user.name) ? 'Withdraw as tester' : 'Volunteer to test'}</button>}{user.role === 'mentor' && <button className="btn primary" onClick={() => { togglePerson(project.id, 'mentors'); notify('Mentor status updated'); close() }}>{project.mentors.includes(user.name) ? 'Stop mentoring' : 'Mentor this'}</button>}{user.role === 'sponsor' && <><input className="amount-input" type="number" min="50" step="50" value={amount} onChange={(event) => setAmount(event.target.value)} aria-label="Sponsorship amount in Singapore dollars" /><button className="btn primary" onClick={() => { sponsor(project.id, Number(amount)); notify(`Sponsored ${project.title}`); close() }}>Sponsor S${amount}</button></>}{owner && <select className="btn" value={project.stage} onChange={(event) => updateProject(project.id, { stage: event.target.value })}>{Object.entries(STAGES).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select>}<button className="btn" onClick={close}>Close</button></div></div></div> }

export default App